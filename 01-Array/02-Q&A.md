### 🧩 Python Arrays (Lists) — Foundation Test

#### 🧠 Level 1: Basics (Warm-up)

1️⃣ Create a list of 5 numbers and print the **sum** of all elements without using the `sum()` function.
→ *(Hint: use a loop or `enumerate()`)*

2️⃣ Given `arr = [2, 4, 6, 8, 10]`, print all **even numbers**.

3️⃣ Given `arr = [3, 1, 7, 9, 2]`, find the **maximum element** manually (without `max()`).

4️⃣ Replace every number greater than 5 in a list with `"High"`.
Example: `[4, 7, 2, 9]` → `[4, "High", 2, "High"]`

5️⃣ Reverse a list **without using** `.reverse()` or slicing `[::-1]`.

---

#### 🧩 Level 2: Iteration + Comprehension Logic

6️⃣ Print both **index and value** of each element in `arr = [10, 20, 30]` using `enumerate()`.

7️⃣ Given `nums = [1, 2, 3, 4, 5]`, create a **new list with each number squared** using a **list comprehension**.

8️⃣ Filter out all **negative numbers** from `arr = [5, -3, 2, -1, 0]` using list comprehension.

9️⃣ Given `matrix = [[1,2,3], [4,5,6]]`, print every element using **nested loops**.

🔟 Given `arr = [1, 2, 3, 4, 5]`, calculate the **sum of only even numbers** using a list comprehension.

---

Would you like me to:

* ✅ **(A)** Check your answers one by one (interactive test mode),
  or
* 📘 **(B)** Give you the detailed solutions + reasoning for all 10 first?
